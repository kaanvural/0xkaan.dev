---
title: "toolname.sh"        # reads like a command / filename
repo: "https://github.com/0xkaan/toolname"   # optional: omit the line if no public repo
status: wip                 # wip | active | stable | archived
order: 10                   # sort order on the page, lower = first
tags: [bash, recon]         # lowercase, short, no spaces
summary: "One line: what it does and why it exists."
sample: false               # leave false for real content
---

One short paragraph: what the tool does, why you built it, and when you reach
for it.

## Usage

```bash
./toolname.sh target.com
```
